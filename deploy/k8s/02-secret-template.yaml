# MODELO DE SECRETS PARA PRODUCAO
# AVISO: Nunca comite valores reais em texto plano neste arquivo.
# Utilize SealedSecrets, HashiCorp Vault ou AWS Secrets Store CSI Driver em producao.
apiVersion: v1
kind: Secret
metadata:
  name: gamelog-secrets
  namespace: gamelog
  labels:
    app.kubernetes.io/name: gamelog
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_WITH_STRONG_POSTGRES_PASSWORD"
  JWT_SECRET: "REPLACE_WITH_MINIMUM_32_CHAR_LONG_SECRET_KEY_FOR_JWT_SIGNING"
  RAWG_API_KEY: "REPLACE_WITH_RAWG_API_KEY"
